# ITDA Fonts

Place `.ttf`, `.otf`, `.woff`, or `.woff2` font files here.
ITDA loads this folder on ComfyUI server startup and exposes the fonts in Text Clip properties.

Do not redistribute commercial font files unless the license allows it.
